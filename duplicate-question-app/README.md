# Duplicate Question Detector (Bi-LSTM)

This project uses a Bi-LSTM model to detect duplicate questions.

## How to Run

1. Install dependencies
   pip install -r requirements.txt

2. Start API
   uvicorn app:app --reload

3. Start UI
   streamlit run streamlit_app.py
